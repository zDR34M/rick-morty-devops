apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rick-morty
spec:
  rules:
    - host: rick-morty.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: rick-morty
                port:
                  number: 80
